/* ============================================================
   Muster Action Center list — modern & bold, light theme
   ------------------------------------------------------------
   Targets your existing classes only:
     .muster-action-center-container  (outer wrapper)
     .muster-action-center-list       (the list)
     .muster-action-center-item       (each row/card)
     .muster-action-center-link       (the clickable link)

   Everything is scoped under #muster-list so these rules
   out-specify Muster's injected default styles WITHOUT needing
   !important. If a rule still doesn't take, the widget is
   likely setting it inline — tell me which property and I'll
   bump it.

   The descendant rules (title / text / img / arrow) are
   defensive: they style those elements if the link contains
   them, and harmlessly do nothing if it doesn't.
   ============================================================ */

#muster-list {
  --m-surface:     #ffffff;  /* card background          */
  --m-ink:         #0b0f1a;  /* primary text             */
  --m-ink-soft:    #5b6478;  /* secondary text           */
  --m-line:        #e7eaf0;  /* borders / dividers       */
  --m-accent:      #4f46e5;  /* bold accent (change me)  */
  --m-accent-weak: #eef0fe;  /* accent tint              */
  --m-radius:      16px;
  --m-shadow:
    0 1px 2px rgba(11, 15, 26, 0.04),
    0 10px 24px -14px rgba(11, 15, 26, 0.20);
  --m-font:
    "Inter", system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
}

/* ---- Container ---- */
#muster-list .muster-action-center-container {
  box-sizing: border-box;
  width: 100%;
  font-family: var(--m-font);
  color: var(--m-ink);
  line-height: 1.45;
}

#muster-list .muster-action-center-container *,
#muster-list .muster-action-center-container *::before,
#muster-list .muster-action-center-container *::after {
  box-sizing: border-box;
}

/* ---- List ---- */
#muster-list .muster-action-center-list {
  list-style: none;     /* in case it renders as a <ul> */
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

/* ---- Item (card) ---- */
#muster-list .muster-action-center-item {
  list-style: none;
  position: relative;
  background: var(--m-surface);
  border: 1px solid var(--m-line);
  border-radius: var(--m-radius);
  box-shadow: var(--m-shadow);
  overflow: hidden;
  transition: transform 150ms ease, box-shadow 150ms ease,
              border-color 150ms ease;
}

/* the one bold move: an accent edge that appears on hover.
   It's always present but transparent, so nothing shifts. */
#muster-list .muster-action-center-item::before {
  content: "";
  position: absolute;
  inset: 0 auto 0 0;
  width: 4px;
  background: transparent;
  transition: background 150ms ease;
}

#muster-list .muster-action-center-item:hover {
  transform: translateY(-2px);
  border-color: var(--m-accent-weak);
  box-shadow:
    0 1px 2px rgba(11, 15, 26, 0.04),
    0 18px 36px -16px rgba(11, 15, 26, 0.26);
}

#muster-list .muster-action-center-item:hover::before {
  background: var(--m-accent);
}

/* ---- Link (fills the card, holds the content) ---- */
#muster-list .muster-action-center-link {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 18px 20px;
  color: var(--m-ink);
  text-decoration: none;
  font-weight: 700;
  letter-spacing: -0.01em;
  transition: color 150ms ease;
}

#muster-list .muster-action-center-link:hover {
  color: var(--m-accent);
}

#muster-list .muster-action-center-link:focus-visible {
  outline: 2px solid var(--m-accent);
  outline-offset: -2px;
  border-radius: var(--m-radius);
}

/* trailing arrow affordance, nudges on hover */
#muster-list .muster-action-center-link::after {
  content: "\2192";                 /* → */
  margin-left: auto;
  flex: none;
  font-weight: 700;
  color: var(--m-accent);
  transition: transform 150ms ease;
}

#muster-list .muster-action-center-link:hover::after {
  transform: translateX(4px);
}

/* ---- Defensive descendant styling (only if present) ---- */

/* a thumbnail / icon inside the link */
#muster-list .muster-action-center-link img {
  flex: none;
  width: 48px;
  height: 48px;
  object-fit: cover;
  border-radius: 10px;
}

/* a title element inside the link */
#muster-list .muster-action-center-link h2,
#muster-list .muster-action-center-link h3,
#muster-list .muster-action-center-link h4 {
  margin: 0;
  font-size: 1.05rem;
  font-weight: 800;
  letter-spacing: -0.02em;
  color: inherit;
}

/* supporting text inside the link */
#muster-list .muster-action-center-link p,
#muster-list .muster-action-center-link span {
  margin: 0;
  font-weight: 500;
  color: var(--m-ink-soft);
}

/* ---- Accessibility floor ---- */
@media (prefers-reduced-motion: reduce) {
  #muster-list .muster-action-center-item,
  #muster-list .muster-action-center-link,
  #muster-list .muster-action-center-link::after {
    transition: none;
  }
}
